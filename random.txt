
  rows = table.find_all('tr')[1:]  # Skip the header row
for row in rows:
        cols = row.find_all('td')
        if len(cols) >= 3:
            song_ranking = cols[1].get_text()
            top_daily_songs.append(song_ranking)
            
            #song_ranking_name = cols[2].get_text()
            #daily_streams.append(float(daily_streams_value.replace(',', '')))
                    
        if len(top_daily_songs) >= 100:
            break